#1 SELECT * FROM titleauthor WHERE title_id = (SELECT TOP 1 title_id FROM titles ORDER BY ytd_sales DESC);
#2 SELECT SUM(advance) FROM titles WHERE title_id IN (SELECT title_id FROM titleauthor WHERE au_id IN (SELECT au_id FROM titleauthor WHERE title_id = 'PS2091'));
#3 
#4 
#5 
